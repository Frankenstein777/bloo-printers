import AquariumGame from '@/components/AquariumGame'

export const metadata = {
    title: 'Aquarium — Octoplans',
    description: 'Watch the fish swim — or hunt them in a 60-second challenge.',
}

export default function AquariumPage() {
    return <AquariumGame />
}
